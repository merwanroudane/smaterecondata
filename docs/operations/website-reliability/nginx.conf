# Staged proposal. PRODUCTION CANDIDATE; not deployed by generator.
user www-data;
worker_processes auto;
# Retire old generations after allowing active requests to finish.
worker_shutdown_timeout 300s;
worker_rlimit_nofile 8192;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log warn;

events {
    # Operating ceiling, not a benchmark or promise of backend capacity.
    worker_connections 4096;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log /var/log/nginx/access.log;
    sendfile on;
    tcp_nopush on;
    server_names_hash_bucket_size 64;
    server_tokens off;
    # Apache 2.4.52 has no configured LimitRequestBody: avoid introducing 1MiB.
    client_max_body_size 0;
    proxy_buffering off;
    proxy_request_buffering off;
    proxy_cache off;
    proxy_intercept_errors off;
    keepalive_timeout 65s;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:DHE-RSA-CHACHA20-POLY1305;
    ssl_ecdh_curve X25519:prime256v1:secp384r1;
    ssl_prefer_server_ciphers off;
    ssl_session_tickets off;
    ssl_session_cache shared:website_tls:10m;
    ssl_session_timeout 1d;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }
    # Empty keys do not consume slots: message POSTs remain independent.
    map "$request_method:$uri" $mcp_global_key {
        default "";
        ~^GET:/mcp/?$ mcp;
    }
    map "$request_method:$uri" $mcp_client_key {
        default "";
        ~^GET:/mcp/?$ $binary_remote_addr;
    }
    limit_conn_zone $mcp_global_key zone=mcp_global:1m;
    limit_conn_zone $mcp_client_key zone=mcp_client:1m;
    # Snapshot of the MCP service's effective CORS allowlist, reviewed with its settings.
    map $http_origin $mcp_allowed_origin {
        default "";
        "http://localhost:3001" $http_origin;
        "https://www.localhost:3001" $http_origin;
    }
    map $mcp_allowed_origin $mcp_cors_credentials {
        default true;
        "" "";
    }
    map $mcp_allowed_origin $mcp_cors_exposed {
        default "Retry-After";
        "" "";
    }

    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name _;
        return 444;
    }
    server {
        listen 443 ssl default_server;
        listen [::]:443 ssl default_server;
        server_name _;
        ssl_certificate /etc/letsencrypt/live/hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/hansearch.com/privkey.pem;
        return 444;
    }
    server {
        listen 80;
        listen [::]:80;
        server_name hansearch.com www.hansearch.com app.hansearch.com agent.hansearch.com n8n.hansearch.com luhan.io www.luhan.io luhanecon.com www.luhanecon.com jiaqili.io www.jiaqili.io jp.hansearch.com api.hansearch.com crypto.hansearch.com github.com/merwanroudane/smaterecondata github.com/merwanroudane/smaterecondata localhost:3001 deepecon.ai www.deepecon.ai trade.hansearch.com;
        location ^~ /.well-known/acme-challenge/ {
            root /var/www/letsencrypt;
            default_type text/plain;
            try_files $uri =404;
        }
        location / {
            proxy_pass http://127.0.0.1:8081;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto http;
            proxy_set_header X-Forwarded-Port 80;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade "";
            proxy_set_header Connection "";
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name hansearch.com www.hansearch.com app.hansearch.com agent.hansearch.com n8n.hansearch.com luhan.io www.luhan.io luhanecon.com www.luhanecon.com jiaqili.io www.jiaqili.io;
        ssl_certificate /etc/letsencrypt/live/hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/hansearch.com/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name jp.hansearch.com;
        ssl_certificate /etc/letsencrypt/live/jp.hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/jp.hansearch.com/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name api.hansearch.com;
        ssl_certificate /etc/letsencrypt/live/api.hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.hansearch.com/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name crypto.hansearch.com;
        ssl_certificate /etc/letsencrypt/live/crypto.hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/crypto.hansearch.com/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name github.com/merwanroudane/smaterecondata github.com/merwanroudane/smaterecondata;
        ssl_certificate /etc/letsencrypt/live/github.com/merwanroudane/smaterecondata/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/github.com/merwanroudane/smaterecondata/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name localhost:3001;
        ssl_certificate /etc/letsencrypt/live/github.com/merwanroudane/smaterecondata/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/github.com/merwanroudane/smaterecondata/privkey.pem;
        location = /mcp {
            # No URI suffix: preserve /mcp, subpaths, and query strings exactly.
            proxy_pass http://127.0.0.1:3002;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade "";
            proxy_set_header Connection "";
            proxy_connect_timeout 5s;
            proxy_read_timeout 330s;
            proxy_send_timeout 330s;
            proxy_ignore_client_abort off;
            proxy_cache off;
            limit_conn mcp_global 40;
            limit_conn mcp_client 20;
            # Both edge ceilings use 503; application per-client ceiling uses 429.
            limit_conn_status 503;
            error_page 503 = @mcp_capacity;
            add_header Content-Security-Policy "frame-ancestors 'self' https://github.com/merwanroudane/smaterecondata https://github.com/merwanroudane/smaterecondata" always;
            add_header X-Content-Type-Options nosniff always;
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
            add_header X-XSS-Protection "1; mode=block" always;
        }
        location ^~ /mcp/ {
            # No URI suffix: preserve /mcp, subpaths, and query strings exactly.
            proxy_pass http://127.0.0.1:3002;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade "";
            proxy_set_header Connection "";
            proxy_connect_timeout 5s;
            proxy_read_timeout 330s;
            proxy_send_timeout 330s;
            proxy_ignore_client_abort off;
            proxy_cache off;
            limit_conn mcp_global 40;
            limit_conn mcp_client 20;
            # Both edge ceilings use 503; application per-client ceiling uses 429.
            limit_conn_status 503;
            error_page 503 = @mcp_capacity;
            add_header Content-Security-Policy "frame-ancestors 'self' https://github.com/merwanroudane/smaterecondata https://github.com/merwanroudane/smaterecondata" always;
            add_header X-Content-Type-Options nosniff always;
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
            add_header X-XSS-Protection "1; mode=block" always;
        }
        # Only locally generated 503s enter here; upstream errors are not intercepted.
        location @mcp_capacity {
            default_type application/json;
            add_header Retry-After 30 always;
            add_header Cache-Control no-store always;
            add_header Access-Control-Allow-Origin $mcp_allowed_origin always;
            add_header Access-Control-Allow-Credentials $mcp_cors_credentials always;
            add_header Access-Control-Expose-Headers $mcp_cors_exposed always;
            add_header Vary Origin always;
            add_header Content-Security-Policy "frame-ancestors 'self' https://github.com/merwanroudane/smaterecondata https://github.com/merwanroudane/smaterecondata" always;
            add_header X-Content-Type-Options nosniff always;
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
            add_header X-XSS-Protection "1; mode=block" always;
            return 503 '{"detail":"MCP stream capacity reached. Retry after closing unused sessions."}';
        }
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 330s;
            proxy_send_timeout 330s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name deepecon.ai www.deepecon.ai;
        ssl_certificate /etc/letsencrypt/live/deepecon.ai/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/deepecon.ai/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name trade.hansearch.com;
        ssl_certificate /etc/letsencrypt/live/trade.hansearch.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/trade.hansearch.com/privkey.pem;
        location / {
            proxy_pass https://127.0.0.1:8444;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Port 443;
            # This is the public edge: discard client-supplied forwarding chains.
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Forwarded "";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_ssl_verify on;
            proxy_ssl_verify_depth 3;
            proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
            proxy_connect_timeout 5s;
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
        }
    }
}
